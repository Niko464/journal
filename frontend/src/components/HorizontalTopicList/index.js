import styled from "styled-components"

const HorizontalTopicList = styled.div(({ theme }) => `
display: flex;
flex-flow: row wrap;
padding-left: 10px;
padding-right: 10px;
`)

export default HorizontalTopicList;